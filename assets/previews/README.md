# Generated previews

These PNG files are extracted directly from the executed notebook outputs by
`scripts/export_readme_previews.py`. Do not edit them independently.

The same script composes `assets/finllm_suite_demo.gif` and
`assets/github_social_preview.png`. Release verification checks that the
expected assets exist and are valid images.
